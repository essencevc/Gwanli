# Environment Configuration

# REQUIRED (always needed for AI processing):
ANTHROPIC_API_KEY=your_anthropic_api_key

# CHOOSE ONE search method:

# Option 1: ChromaDB embedding search (more powerful, requires OpenAI)
CHROMA_API_KEY=your_chroma_api_key
CHROMA_TENANT=your_chroma_tenant
CHROMA_DATABASE=your_chroma_database
OPENAI_API_KEY=your_openai_api_key

# Option 2: SQLite full-text search (simpler, local)
SQLITE_PATH=~/.vibeall/db.sqlite  # Optional, this is the default
